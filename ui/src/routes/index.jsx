// Importa a biblioteca React.
import React from "react";
import AuthRoutes from "./app.auth";

// Define um componente funcional chamado Routes.
const Routes = () => (
        <AuthRoutes />
);

// Exporta o componente Routes como o valor padrão deste módulo.
export default Routes;
